[package]
name = "turn-provider-azure-openai"
version = "0.5.0-alpha"
edition = "2021"
description = "Official Azure OpenAI Inference Driver for Turn"

[dependencies]
reqwest = { version = "0.11", features = ["blocking", "json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
