[package]
name = "turn-provider-aws-anthropic"
version = "0.5.0-alpha"
edition = "2021"
description = "Official AWS Bedrock Anthropic Inference Driver for Turn"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
