[package]
name = "turn-provider-openai"
version = "2.0.0"
edition = "2021"
description = "Official OpenAI Inference Driver for Turn (standard api.openai.com)"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
