[package]
name = "turn-provider-anthropic"
version = "2.0.0"
edition = "2021"
description = "Official Anthropic Inference Driver for Turn (api.anthropic.com)"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
