// Hello Turn - minimal program
// Run: cargo run -- run examples/hello.turn

turn {
  let name = "Turn";
  remember("user", name);
  context.append("Hello, " + name);
  let out = call("echo", "Hello");
  return out;
}
