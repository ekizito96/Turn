// Conditionals: if/else with == and recall
// Run: cargo run -- run examples/conditionals.tn

turn {
  let weather = call("echo", "sunny");
  remember("weather", weather);
  let w = recall("weather");
  if w == "sunny" {
    return "Nice day!";
  } else {
    return "Stay dry!";
  }
}
